services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: cutover
      POSTGRES_PASSWORD: cutover
      POSTGRES_DB: cutover
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
